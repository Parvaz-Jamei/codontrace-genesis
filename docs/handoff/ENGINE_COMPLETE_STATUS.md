# Engine-complete status — 2026-09-25

## English

All eight engine-complete gates are **PASS** in code + `tests/engine_complete/`. HostParasiteWorld now runs birth/death on both populations, whole-pop attachment with fail census, core ATP ledger coupling, core RNG+Mutation on both sides, InheritAttached on birth, bit-identical replay, and demographic effects only at `n≥30`. Registry participates via `HostParasiteGenesisPath` alongside GenesisEngine — life_loop/`engine.py` stay domain-free; no infection in the kernel. ClaimGate and BAIC pins untouched. Open-problem / innovation-meter work remains **blocked** until owner unlocks the next lock step.

## فارسی

هر هشت دروازهٔ تمام‌شدن موتور در کد و تست **سبز** است. `HostParasiteWorld` حالا تولد/مرگ هر دو جمعیت، اتصال کمّی کل جمعیت با سرشماری شکست، انرژی از دفتر ATP هسته، جهش با Mutation+RNG هسته، ارث‌بری روی تولد، replay بیت‌یکسان، و اثر جمعیتی فقط با `n≥30` را دارد. رجیستری از مسیر `HostParasiteGenesisPath` کنار GenesisEngine شرکت می‌کند؛ هسته و life_loop دامنه‌آزاد مانده‌اند و فیزیک عفونت داخل `engine.py` نیست. ClaimGate و پینهای BAIC دست‌نخورده‌اند. کار مسئلهٔ باز / متر نوآوری هنوز **قفل** است تا مالک گام بعدی را باز کند.
