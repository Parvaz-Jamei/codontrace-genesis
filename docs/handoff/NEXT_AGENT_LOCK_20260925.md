# Next-agent lock — CodonTrace Genesis (2026-09-25)

**Status:** Binding. Order is locked. Do not reorder.

## Locked order

1. **Complete the engine** (gates below must be true in *code*, not docstring)
2. **Open literature problem** (why still unsolved; verified DOI only)
3. **Innovation** (from another field only if it changes loop *mechanism*)
4. **Test** (SF1–SF8 style fields; honest FAIL allowed)

## Forbidden until engine-complete

- Building “unsolved challenge” tests
- Claims like “best in the world” or “solved what nobody solved”
- Softening ClaimGate refuses (`red_queen_proved`, etc.)
- Infection physics inside `engine.py`
- Guessed DOIs
- Using the current innovation-campaign pattern (β₁ on handmade graphs, Fisher between two fabricated maps, DiD on a static census) as the template for next work — those landed *before* engine completion

## Claim ceiling

Digital results are analogues. Ceiling stays `runtime_observation` / `candidate_evidence`.

## Engine-complete gates (all required)

- `tick` changes **both** populations with birth and death; extinction and coexistence both recorded
- Mutation on **both** sides uses core mutation + RNG — not a separate hash path
- Quantitative attachment with capacity and fail reasons for the **whole** population, not only the first pair
- Energy from the **core energy ledger**; tradeoffs from parameters, not hard-coded coefficients
- Inheritance actually invoked — `InheritAttachedPolicy` must be called in `tick` (today it is constructed and unused)
- Registry wired into the `GenesisEngine` life-loop — a parallel book is a second engine
- Bit-identical replay
- Main effect only with `n ≥ 30`

## After engine-complete — problem seeds (starting points, not final list)

- Separate oscillatory Red Queen from pure arms race via time-shift  
  Decaestecker 2007 doi:10.1038/nature06291 ; Brockhurst 2014 doi:10.1098/rspb.2014.1382 ; Buckingham 2022 doi:10.1111/jeb.13981
- Virulence–transmission tradeoff still under-evidenced empirically  
  Alizon 2009 doi:10.1111/j.1420-9101.2008.01658.x ; Froissart 2010 doi:10.1098/rstb.2010.0068 ; scarce empirical eLife doi:10.7554/eLife.20492
- Maintenance of recombination under coevolving parasites  
  Morran 2011 doi:10.1126/science.1206360
- Any other problem only with a verified DOI

## Innovation rule

Borrow from another discipline only if it changes loop mechanism (branching-process extinction, time-shift causality, contact network for transmission, Pareto front for tradeoffs). New meters on frozen outputs are not innovation.

## Test record fields (required)

`doi`, `literature_prediction`, `why_unsolved`, `success_criterion`, null arms (content / structure / dual), `n_seeds`, `hypothesis_supported` separate from engineering green, `honesty`, `claim_ceiling`, result JSON.

If the engine is still shallow and an “unsolved challenge” test is written, that test is rejected even if it passes.

## Persian owner note (carry forward)

کار باید دقیق و جامع باشد. «بهترین» یعنی دینامیک واقعی و ابطال‌پذیر روی همین موتور. تا دروازهٔ تمام‌شدن سبز نشده هیچ تست حل‌نشده نساز. کمپین‌های نوآوری فعلی الگوی کار بعدی نیستند.
