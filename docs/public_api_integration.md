# Integration Public API Policy

Policy A is used: stable scientific GENESIS APIs are documented under `codontrace.genesis`.
Root-level `codontrace` remains a convenience API and must not be required for
normal scientific evidence work. Provisional Phase-B aliases are not claim-ready
by default, while legacy names remain importable when backward compatibility
requires it.
