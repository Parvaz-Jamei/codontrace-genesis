# کارگاه طراحی — حسابرسی کامل‌بودن موج ۵ (خلاصه فارسی)

**پروژه:** CodonTrace Genesis  
**پایه:** `main` در SHA `f093ed826876b916a140c39e9bba1ef7baeff257` (ادغام اسکواش #43–#46)  
**شاخه حسابرسی:** `docs/host-parasite-wave5-completeness-audit`

این سند خلاصهٔ پنج دور کارشناسی انگلیسی
(`DEPTH_BRAINSTORM_WAVE5_COMPLETENESS.md`) برای تصمیم‌گیری گام بعدی است.

## نقش‌ها

- **A** فرگشت دیجیتال / ALife  
- **B** صداقت میکروبی / ژنوم  
- **C** ClaimGate / راستی‌آزمایی علّی  

## دور ۱ — فهرست و شکار نقص

موج ۵ (فازهای ۱۸–۲۳) روی main نشسته: رجیستری attach نرم‌کامل، گذار ARD→FSD،
فاکتوریل منابع×دینامیک، contingency چندبذری، Cornish ترتیبی، mutator اسکَنلان.

**نقص‌های مشخص:**

1. **مسدودکننده:** عنوان اشتباه مقاله Lopez Pascua 2014 در
   `evidence/hall_lopez_pascua_ard_fsd.md` (عنوان واقعی: Higher resources
   decrease fluctuating selection…؛ DOI درست بود). عنوان Hall هم ناقص بود.
2. **مسدودکننده:** فاز ۲۳ بدون یادداشت شواهد اختصاصی Scanlan 2015.
3. **مسدودکننده (V&V):** کلیدهای `WAVE5_ATTACH_KEYS` بدون assert سیم‌کشی به
   توابع `attach_*`.
4. **اسنادی:** DOIهای Hall/Lopez در ردیف ARD/FSD ماتریس مقایسه‌گر ناقص بود.

قفل‌های سخت سالم بودند: یک DomainProfile، بدون عفونت در `engine.py`، پینهای
BAIC دست‌نخورده، بدون `population/`، بدون ادعای Red Queen / درمان فاژی / CRISPR.

## دور ۲ — فشار ادبیات

DOIهای واقعی تأیید شد (Hall 2011، Lopez Pascua 2014، Scanlan 2015، Koskella
2014، Zaman 2014، Fortuna 2021، Cornish، MODES، Channon، Goldsby، JaxLife).
فشار کیفیت: پروتکل‌های ClaimGate درست‌اند؛ باقی‌ماندهٔ کار بسته‌بندی صداقت
شواهد و سیم‌کشی attach است — نه فیزیک عفونت جدید و نه پورت JaxLife/Avida.

## دور ۳ — اصلاح یا رد

| نقص | تصمیم |
|-----|--------|
| عنوان شواهد Hall/Lopez | **اصلاح** |
| یادداشت Scanlan | **اصلاح** |
| assert سیم‌کشی موج ۵ | **اصلاح** + تست |
| DOI ماتریس | **اصلاح** |
| هم‌تکاملی زنده Avida-مقیاس داخل فاز ۱۹/۲۰ | **رد** (خزش موتور / تئاتر هویت) |
| MLS / OEE ثابت‌شده / درمان / CRISPR / پروفایل جدا / پین BAIC | **رد** (قفل سخت) |

## دور ۴ — دروازه کامل‌بودن

**آیا موج ۵ برای این برش پورت soft-complete است؟**  
**بله، با شرط** اجرای اصلاحات P0/P1 بالا، سبز بودن pytest، پین‌های BAIC، و
diff خالی `engine.py` نسبت به main.

- **باید قبل از اعلام اتمام موج ۵:** D1–D4 (در همین PR حسابرسی)  
- **خوب است ولی ضروری نیست:** اتصال غنی‌تر env زنده؛ re-export در
  `adapters/__init__`  
- **خارج از محدوده:** عفونت در engine، DomainProfile جدا، MLS، صادرات هویت
  Avida، JaxLife HP، چندصندلی co-infection، HGT/CRISPR/درمان/BSL/بالینی، اثبات
  Red Queen یا OEE-MODES

## دور ۵ — امضا و بذرهای موج ۶ (فقط اگر شایسته)

نمرات پس از اصلاح: A/B/C همگی soft-complete را می‌پذیرند (کیفیت A کمی کمتر به‌خاطر
assayهای ترکیبی دیجیتال، نه هم‌تکاملی زنده).

**بذرهای موج ۶ که از پنج دور جان سالم به‌در بردند:**

- **W6-a** بسته دود یک‌جا: attach ترتیبی فازهای ۱۸–۲۳ روی یک bundle با audit نردبان  
- **W6-b** یادداشت تازه‌سازی digestهای قفل‌شده HE_HP بدون دست زدن به پین BAIC  
- **W6-c** (اختیاری) پل contingency بین فاز ۱۱ (آنتروپی) و فاز ۲۱ بدون ادعای بیولوژی جدید  

بذرهای کشته‌شده: موتور MLS، ادعای OEE/MODES passed، عفونت در engine، و بقیهٔ
فهرست رد.

## اصلاحات انجام‌شده در همین شاخه

- اصلاح عنوان Hall/Lopez در شواهد + اشاره به فاز ۱۹/۲۰  
- افزودن `evidence/scanlan_2015_mutator_abiotic.md` و ردیف README شواهد  
- `WAVE5_ATTACH_CALLABLES` + `assert_wave5_attach_surface_wired` + تست فاز ۱۸  
- DOI و فرمت `COMPARATOR_MATRIX`  

## تصمیم پیشنهادی برای شما

1. ادغام همین PR حسابرسی به main → موج ۵ رسماً soft-complete.  
2. اگر می‌خواهید ادامه دهید: فقط بذرهای W6-a/b (و اختیاری c) را باز کنید؛ بقیه را
   باز نکنید.  
3. اگر اولویت جای دیگر است (مثلاً HE یا کشف باز)، موج ۶ را عقب بیندازید — قفل‌های
   host–parasite پایدارند.
